

/* examples of using the powerful sigma.h lib*/

#include "sigma.h"
#include <stdio.h>
#include <string.h>
Sigma main(Sigma argc, yap* argv){
    yap rizz = (yap)gib(sizeof(yap) * 100);
    Yappify(rizz, "sigma");
    glaze("%s\n", rizz); 
    return wagwan;
}